/**
 * The Detecto plate is lettered in a bold condensed grotesque — the face the reference
 * drawing uses throughout, and the reason its numerals can be that large and still fit
 * between the notches. It has to be a real lowercase: the plate reads "lb." and "kg"
 * and "CAP.-440lb.x4oz", so a small-caps face would set them wrong.
 *
 * Declared here rather than on each part: the design sets it once on the stage and
 * every drawing inherits it.
 */
export const DETECTO_TYPEFACE =
  "'Roboto Condensed', 'Arial Narrow', 'Helvetica Neue', Arial, sans-serif"
